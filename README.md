# algorithm-world

A coursework which implements a simple microservices architecture. 

The application goal is to store and display articles. The articles might contain executable snippets of code, which are executable in their sandboxed environment.

